  <h2><%= @post.title %></h2>
  <p><%= @post.body %></p>
  <p><%= @post.tags %></p>
  <td><%= link_to "Edit", edit_user_post_path(@post.user, @post) %></td>
  <td><%= link_to "Delete", [@post.user, @post], method: :delete, data: { confirm: "Are you sure you want to delete this post?"} %></td>